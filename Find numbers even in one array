class Solution(object):
    a=0
    b=0
    def findNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        b=0
        for v in nums:
            a=len(str(v))
            if a%2 == 0:
                b=b+1
        return b    
          
